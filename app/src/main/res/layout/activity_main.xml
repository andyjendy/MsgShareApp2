<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity"
    android:padding="10dp">

    <Button
        android:id="@+id/btnShowToast"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="63dp"
        android:text="Show Toast" />

    <EditText
        android:id="@+id/etUserMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnShowToast"
        android:layout_marginTop="65dp"
        android:ems="10"
        android:hint="Wpisz tu cosik" />

    <Button
        android:id="@+id/btnStartSecondActivity"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/etUserMessage"
        android:layout_marginTop="76dp"
        android:text="Send Data do drugiej aktywnosci" />

    <Button
        android:id="@+id/btnShareToApp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnStartSecondActivity"
        android:layout_marginTop="36dp"
        android:text="Share do innej aplikacji" />

    <Button
        android:id="@+id/btnRecyclerViewDemo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/btnShareToApp"
        android:layout_marginTop="98dp"
        android:text="Recycler View Demo"
        android:shadowColor="#BC0000"
        android:textColor="#FFFFFF"/>

</RelativeLayout>